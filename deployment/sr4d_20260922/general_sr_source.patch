diff --git a/gaussian_renderer/__init__.py b/gaussian_renderer/__init__.py
index 23dd90b..6d2de43 100644
--- a/gaussian_renderer/__init__.py
+++ b/gaussian_renderer/__init__.py
@@ -33,7 +33,7 @@ def quaternion_multiply(q1, q2):
 
 
 def render(viewpoint_camera, pc: GaussianModel, pipe, bg_color: torch.Tensor, d_xyz, d_rotation, d_scaling, is_6dof=False,
-           scaling_modifier=1.0, override_color=None):
+           scaling_modifier=1.0, override_color=None, render_high=True):
     """
     Render the scene.
 
@@ -75,7 +75,7 @@ def render(viewpoint_camera, pc: GaussianModel, pipe, bg_color: torch.Tensor, d_
         bg=bg_color,
         scale_modifier=scaling_modifier,
         viewmatrix=viewpoint_camera.world_view_transform,
-        projmatrix=viewpoint_camera.full_proj_transform,
+        projmatrix=getattr(viewpoint_camera, 'full_proj_transform_lr', viewpoint_camera.full_proj_transform),
         sh_degree=pc.active_sh_degree,
         campos=viewpoint_camera.camera_center,
         prefiltered=False,
@@ -142,7 +142,7 @@ def render(viewpoint_camera, pc: GaussianModel, pipe, bg_color: torch.Tensor, d_
         opacities=opacity,
         scales=scales,
         rotations=rotations,
-        cov3D_precomp=cov3D_precomp)[0]
+        cov3D_precomp=cov3D_precomp)[0] if render_high else None
 
     # Those Gaussians that were frustum culled or had a radius of 0 were not visible.
     # They will be excluded from value updates used in the splitting criteria.
@@ -549,4 +549,4 @@ def save_max_axis_stats(pc: GaussianModel, out_dir: str,
     out_path.parent.mkdir(parents=True, exist_ok=True)
     with open(out_path, "w") as f:
         json.dump(stats, f, indent=2)
-    # print(f"[STATS] {stats}  ->  {out_path}")
\ No newline at end of file
+    # print(f"[STATS] {stats}  ->  {out_path}")
diff --git a/metrics.py b/metrics.py
index 7a6ce7a..6564237 100644
--- a/metrics.py
+++ b/metrics.py
@@ -27,7 +27,9 @@ def readImages(renders_dir, gt_dir):
     renders = []
     gts = []
     image_names = []
-    for fname in os.listdir(renders_dir):
+    for fname in sorted(os.listdir(renders_dir)):
+        if not fname.lower().endswith('.png'):
+            continue
         render = Image.open(renders_dir / fname)
         gt = Image.open(gt_dir / fname)
         renders.append(tf.to_tensor(render).unsqueeze(0)[:, :3, :, :].cuda())
@@ -75,7 +77,7 @@ def evaluate(model_paths):
             for idx in tqdm(range(len(renders)), desc="Metric evaluation progress"):
                 ssims.append(ssim(renders[idx], gts[idx]))
                 psnrs.append(psnr(renders[idx], gts[idx]))
-                lpipss.append(lpips_fn(renders[idx], gts[idx]).detach())
+                lpipss.append(lpips_fn(renders[idx], gts[idx], normalize=True).detach())
 
             print("  SSIM : {:>12.7f}".format(torch.tensor(ssims).mean(), ".5"))
             print("  PSNR : {:>12.7f}".format(torch.tensor(psnrs).mean(), ".5"))
diff --git a/render.py b/render.py
index e59b164..54239f7 100644
--- a/render.py
+++ b/render.py
@@ -340,14 +340,20 @@ def render_sets(dataset: ModelParams, iteration: int, pipeline: PipelineParams,
                 mode: str):
     with torch.no_grad():
         gaussians = GaussianModel(dataset.sh_degree)
-        scene = Scene(dataset, gaussians, load_iteration=iteration, shuffle=False)
+        scene = Scene(dataset, gaussians, load_iteration=iteration, shuffle=False, stage='hr')
         deform_lr = DeformModel(dataset.is_blender, dataset.is_6dof)
         deform_hr = DeformModel(dataset.is_blender, dataset.is_6dof)
         # ema = EMA(deform.deform, decay=0.999)
         # ema.register()
         # deform.read_ema(ema)
-        deform_lr.load_weights_lr(dataset.model_path)
-        deform_hr.load_weights_hr(dataset.model_path)
+        metadata_path = os.path.join(dataset.model_path, 'deform_hr',
+                                     f'iteration_{scene.loaded_iter}', 'stage_metadata.json')
+        coarse_iteration = -1
+        if os.path.exists(metadata_path):
+            with open(metadata_path) as metadata_file:
+                coarse_iteration = json.load(metadata_file)['coarse_iteration']
+        deform_lr.load_weights_lr(dataset.model_path, coarse_iteration)
+        deform_hr.load_weights_hr(dataset.model_path, scene.loaded_iter)
         # 打印模型参数（应用EMA权重前）
         
         bg_color = [1, 1, 1] if dataset.white_background else [0, 0, 0]
@@ -369,15 +375,15 @@ def render_sets(dataset: ModelParams, iteration: int, pipeline: PipelineParams,
         else:
             render_func = interpolate_all
 
-        # if not skip_train:
-        #     render_func(dataset.model_path, dataset.load2gpu_on_the_fly, dataset.is_6dof, "train", scene.loaded_iter,
-        #                 scene.getTrainCameras(), gaussians, pipeline,
-        #                 background, deform)
-
-        # if not skip_test:
-        render_func(dataset.model_path, dataset.load2gpu_on_the_fly, dataset.is_6dof, "test", scene.loaded_iter,
-                    scene.getTestCameras(), gaussians, pipeline,
-                    background, deform_lr, deform_hr)
+        if mode != 'render':
+            raise NotImplementedError("The upstream interpolation modes accept only one deformation field. "
+                                      "Use --mode render for the validated coarse + residual SR4D path.")
+        if not skip_train:
+            render_func(dataset.model_path, dataset.load2gpu_on_the_fly, dataset.is_6dof, "train", scene.loaded_iter,
+                        scene.getTrainCameras(), gaussians, pipeline, background, deform_lr, deform_hr)
+        if not skip_test:
+            render_func(dataset.model_path, dataset.load2gpu_on_the_fly, dataset.is_6dof, "test", scene.loaded_iter,
+                        scene.getTestCameras(), gaussians, pipeline, background, deform_lr, deform_hr)
 
 
 if __name__ == "__main__":
diff --git a/scene/__init__.py b/scene/__init__.py
index e50be01..8c5e446 100644
--- a/scene/__init__.py
+++ b/scene/__init__.py
@@ -26,7 +26,7 @@ class Scene:
     gaussians: GaussianModel
 
     def __init__(self, args: ModelParams, gaussians: GaussianModel, load_iteration=None, shuffle=True,
-                 resolution_scales=[1.0]):
+                 resolution_scales=[1.0], stage='lr'):
         """b
         :param path: Path to colmap scene main folder.
         """
@@ -34,9 +34,12 @@ class Scene:
         self.loaded_iter = None
         self.gaussians = gaussians
         self.sr_image_path = args.sr_image_path
+        if stage not in ('lr', 'hr'):
+            raise ValueError("stage must be 'lr' or 'hr'")
+        point_cloud_dir = 'point_cloud_hr' if stage == 'hr' else 'point_cloud'
         if load_iteration:
             if load_iteration == -1:
-                self.loaded_iter = searchForMaxIteration(os.path.join(self.model_path, "point_cloud"))
+                self.loaded_iter = searchForMaxIteration(os.path.join(self.model_path, point_cloud_dir))
             else:
                 self.loaded_iter = load_iteration
             print("Loading trained model at iteration {}".format(self.loaded_iter))
@@ -95,7 +98,7 @@ class Scene:
 
         if self.loaded_iter:
             self.gaussians.load_ply(os.path.join(self.model_path,
-                                                 "point_cloud",
+                                                 point_cloud_dir,
                                                  "iteration_" + str(self.loaded_iter),
                                                  "point_cloud.ply"),
                                     og_number_points=len(scene_info.point_cloud.points))
@@ -123,8 +126,11 @@ class Scene:
         )
         return camera
 
-    def save(self, iteration):
-        point_cloud_path = os.path.join(self.model_path, "point_cloud/iteration_{}".format(iteration))
+    def save(self, iteration, stage='lr'):
+        if stage not in ('lr', 'hr'):
+            raise ValueError("stage must be 'lr' or 'hr'")
+        directory = 'point_cloud_hr' if stage == 'hr' else 'point_cloud'
+        point_cloud_path = os.path.join(self.model_path, directory, "iteration_{}".format(iteration))
         self.gaussians.save_ply(os.path.join(point_cloud_path, "point_cloud.ply"))
 
     def getTrainCameras(self, scale=1.0):
diff --git a/scene/cameras.py b/scene/cameras.py
index 4d6d57e..7c4ab42 100644
--- a/scene/cameras.py
+++ b/scene/cameras.py
@@ -102,16 +102,16 @@ class Camera(nn.Module):
                 # edgemap = spatial_gradient(self.original_image[None], order=1)[0].abs().sum(1)
                 data_min = edgemap.min()
                 data_max = edgemap.max()
-                edgemap = (edgemap - data_min) / (data_max - data_min)
+                edgemap = (edgemap - data_min) / (data_max - data_min).clamp_min(1e-8)
                 # pdb.set_trace()
                 # edgemap[self.canny_mask.bool()]=1
-                self.edgemap = edgemap.unsqueeze(0).repeat(3,1,1).cuda()
+                self.edgemap = edgemap.unsqueeze(0).repeat(3,1,1).to(self.data_device)
                 
                 edgemap_hr = spatial_gradient(self.original_image[None].cpu(), order=1)[0].abs().sum(1).mean(0)
                 data_min_hr = edgemap_hr.min()
                 data_max_hr = edgemap_hr.max()
-                edgemap_hr = (edgemap_hr - data_min_hr) / (data_max_hr - data_min_hr)
-                self.edgemap_hr = edgemap_hr.unsqueeze(0).repeat(3,1,1).cuda()
+                edgemap_hr = (edgemap_hr - data_min_hr) / (data_max_hr - data_min_hr).clamp_min(1e-8)
+                self.edgemap_hr = edgemap_hr.unsqueeze(0).repeat(3,1,1).to(self.data_device)
                 # save_tensor_as_png(self.edgemap,"debug/canny")
                 # save_tensor_as_png(self.edgemap_hr,"debug/edgemap")
                 # pdb.set_trace()
@@ -138,6 +138,9 @@ class Camera(nn.Module):
 
     def load2device(self, data_device='cuda'):
         self.original_image = self.original_image.to(data_device)
+        self.downsampled_image = self.downsampled_image.to(data_device)
+        self.edgemap = self.edgemap.to(data_device)
+        self.edgemap_hr = self.edgemap_hr.to(data_device)
         self.world_view_transform = self.world_view_transform.to(data_device)
         self.projection_matrix = self.projection_matrix.to(data_device)
         self.full_proj_transform = self.full_proj_transform.to(data_device)
diff --git a/scene/dataset_readers.py b/scene/dataset_readers.py
index ca24448..c84cc11 100644
--- a/scene/dataset_readers.py
+++ b/scene/dataset_readers.py
@@ -252,7 +252,7 @@ def readCamerasFromTransforms(path, transformsfile, white_background, extension=
             arr = norm_data[:, :, :3] * norm_data[:, :,
                                                   3:4] + bg * (1 - norm_data[:, :, 3:4])
             image = Image.fromarray(
-                np.array(arr * 255.0, dtype=np.byte), "RGB")
+                np.array(arr * 255.0, dtype=np.uint8), "RGB")
 
             fovy = focal2fov(fov2focal(fovx, image.size[0]), image.size[1])
             FovY = fovx
diff --git a/scene/gaussian_model.py b/scene/gaussian_model.py
index 1f827f8..3eb725e 100644
--- a/scene/gaussian_model.py
+++ b/scene/gaussian_model.py
@@ -192,7 +192,7 @@ class GaussianModel:
         attributes = np.concatenate((xyz, normals, f_dc, f_rest, opacities,  texture, scale,rotation), axis=1)
         elements[:] = list(map(tuple, attributes))
         el = PlyElement.describe(elements, 'vertex')
-        PlyData([el]).write(path)
+        PlyData([el], comments=[f"active_sh_degree {self.active_sh_degree}"]).write(path)
 
     def reset_opacity(self):
         opacities_new = inverse_sigmoid(torch.min(self.get_opacity, torch.ones_like(self.get_opacity) * 0.01))
@@ -245,6 +245,9 @@ class GaussianModel:
         self._rotation = nn.Parameter(torch.tensor(rots, dtype=torch.float, device="cuda").requires_grad_(True))
 
         self.active_sh_degree = self.max_sh_degree
+        for comment in plydata.comments:
+            if comment.startswith('active_sh_degree '):
+                self.active_sh_degree = int(comment.split()[1])
 
     def replace_tensor_to_optimizer(self, tensor, name):
         optimizable_tensors = {}
diff --git a/submodules/depth-diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.h b/submodules/depth-diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.h
index bc3f0ec..b323bbb 100644
--- a/submodules/depth-diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.h
+++ b/submodules/depth-diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.h
@@ -10,6 +10,7 @@
  */
 
 #pragma once
+#include <cstdint>
 
 #include <iostream>
 #include <vector>
@@ -71,4 +72,4 @@ namespace CudaRasterizer
 		T::fromChunk(size, P);
 		return ((size_t)size) + 128;
 	}
-};
\ No newline at end of file
+};
diff --git a/submodules/simple-knn/simple_knn.cu b/submodules/simple-knn/simple_knn.cu
index e72e4c9..7b69638 100644
--- a/submodules/simple-knn/simple_knn.cu
+++ b/submodules/simple-knn/simple_knn.cu
@@ -20,7 +20,7 @@
 #include <cuda_runtime_api.h>
 #include <thrust/device_vector.h>
 #include <thrust/sequence.h>
-#define __CUDACC__
+#include <cfloat>
 #include <cooperative_groups.h>
 #include <cooperative_groups/reduce.h>
 
@@ -218,4 +218,4 @@ void SimpleKNN::knn(int P, float3* points, float* meanDists)
 	boxMeanDist << <num_boxes, BOX_SIZE >> > (P, points, indices_sorted.data().get(), boxes.data().get(), meanDists);
 
 	cudaFree(result);
-}
\ No newline at end of file
+}
diff --git a/train.py b/train.py
index 0c9ee44..d12cf62 100644
--- a/train.py
+++ b/train.py
@@ -24,6 +24,7 @@ import uuid
 import time
 from tqdm import tqdm
 from utils.image_utils import psnr
+from utils.tasa_utils import snapshot_gaussians, apply_tasa
 from argparse import ArgumentParser, Namespace
 from arguments import ModelParams, PipelineParams, OptimizationParams
 from scene.gaussian_model import build_scaling_rotation
@@ -56,6 +57,8 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
     deform.train_setting(opt)
 
     scene = Scene(dataset, gaussians)
+    if not scene.getTrainCameras():
+        raise ValueError("The training split contains no cameras")
     gaussians.training_setup(opt)
 
     bg_color = [1, 1, 1] if dataset.white_background else [0, 0, 0]
@@ -139,12 +142,12 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
         # gt_image = viewpoint_cam.original_image.cuda()
         Ll1 = l1_loss(image, gt_image)
         loss = (1.0 - opt.lambda_dssim) * Ll1 + opt.lambda_dssim * (1.0 - ssim(image, gt_image))
-        loss = loss + args.opacity_reg * torch.abs(gaussians.get_opacity).mean()
+        loss = loss + opt.opacity_reg * torch.abs(gaussians.get_opacity).mean()
 
         rendered_tex = render_pkg_edge["texture"]
         
         rendered_tex = rendered_tex.unsqueeze(0)
-        rendered_tex_lr = F.interpolate(rendered_tex, scale_factor=0.25, mode='bilinear', align_corners=False)
+        rendered_tex_lr = F.avg_pool2d(rendered_tex, kernel_size=4, stride=4)
         rendered_tex_lr = rendered_tex_lr.squeeze(0)
 
         mask_ppp = rendered_tex_lr[0].reshape(-1,1)
@@ -181,10 +184,13 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
                                        testing_iterations, scene, render, (pipe, background), deform,
                                        dataset.load2gpu_on_the_fly, dataset.is_6dof)
 
-            if iteration in testing_iterations:
+            if iteration in testing_iterations and cur_psnr is not None:
+                current_psnr = float(cur_psnr)
+                if current_psnr > best_psnr:
+                    best_psnr, best_iteration = current_psnr, iteration
                 psnr_records.append({
                     "iteration": iteration,
-                    "psnr": round(cur_psnr.item(), 4)        # 保留 4 位小数即可
+                    "psnr": round(current_psnr, 4)        # 保留 4 位小数即可
                 })
             # if iteration in testing_iterations:
             #     if cur_psnr.item() > best_psnr:
@@ -194,7 +200,7 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
             if iteration in saving_iterations:
                 print("\n[ITER {}] Saving Gaussians".format(iteration))
                 scene.save(iteration)
-                deform.save_weights_lr(args.model_path, iteration)
+                deform.save_weights_lr(dataset.model_path, iteration)
 
             # Densification
             if iteration < opt.densify_until_iter:
@@ -209,86 +215,26 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
                         dataset.white_background and iteration == opt.densify_from_iter):
                     gaussians.reset_opacity()
 
-            xyz_old=gaussians._xyz
-            scale_old=gaussians._scaling
-            rotation_old=gaussians._rotation
-            opacity_old=gaussians._opacity
-            fea_1 = gaussians._features_dc
-            fea_2 = gaussians._features_rest
-            tex_old=gaussians._texture
-
-            # Optimizer step
+            # TASA must snapshot raw values before Adam mutates the Parameters.
+            # Preserve the upstream coarse-stage 3000-iteration warm-up.
             if iteration < opt.iterations:
+                tasa_enabled = iteration > 3000
+                previous = snapshot_gaussians(gaussians) if tasa_enabled else None
                 gaussians.optimizer.step()
-                gaussians.update_learning_rate(iteration)
+                xyz_lr = gaussians.update_learning_rate(iteration)
                 deform.optimizer.step()
                 gaussians.optimizer.zero_grad(set_to_none=True)
-                deform.optimizer.zero_grad()
+                deform.optimizer.zero_grad(set_to_none=True)
                 deform.update_learning_rate(iteration)
 
-                xyz_lr = gaussians.update_learning_rate(iteration)
-                if not isinstance(xyz_lr, torch.Tensor):
-                    xyz_lr = torch.tensor(xyz_lr, device=tex_old.device)
-
-                tex_new=gaussians.get_texture
-                # pdb.set_trace()
-                mask = tex_new < tex_old
-                mask_better = tex_new > tex_old 
-                prob = torch.ones_like(tex_old, device=tex_old.device)
-
-                if mask.any():
-                    
-                    prob[mask] = (torch.exp(-(tex_old[mask] - tex_new[mask])* opt.tex_rate/xyz_lr))
-                    p_rand = torch.rand_like(prob, device=tex_old.device)
-                    # pdb.set_trace()
-                    p_nom = p_rand < prob
-
-                    # p_nom = torch.tensor([random_choice(p.item()) for p in prob], device=tex_old.device)
-
-                    accepted = mask_better | (mask & p_nom)   # ← 关键
-                    accept_rate = accepted.float().mean().item()
-
-                    if iteration % 100 == 0:
+                if tasa_enabled:
+                    accepted, _ = apply_tasa(gaussians, previous, xyz_lr, opt.tex_rate)
+                    accept_rate = accepted.float().mean().item() if accepted.numel() else 1.0
+                    if iteration % 100 == 0 or iteration == 3001:
                         print(f"[ITER {iteration}] accept_rate = {accept_rate:.6f}")
                         with open(acc_log_path, "a") as f:
                             f.write(f"{iteration},{accept_rate:.6f}\n")
-                    
-                    if p_nom.any() and iteration > 3000 :
-                        # print(p_nom.sum()/p_nom.shape[0])
-                        p_nom = p_nom.squeeze()
-                        gaussians._xyz[~p_nom] = xyz_old[~p_nom]
-                        gaussians._features_dc[~p_nom] = fea_1[~p_nom]
-                        gaussians._features_rest[~p_nom] = fea_2[~p_nom]
-                        gaussians._texture[~p_nom] = tex_old[~p_nom]
-                        gaussians._scaling[~p_nom] = scale_old[~p_nom]
-                        gaussians._rotation[~p_nom] = rotation_old[~p_nom]
-                        gaussians._opacity[~p_nom] = opacity_old[~p_nom]
-                        # indices_to_restore = p_nom.nonzero(as_tuple=True)[0]
-                        # if indices_to_restore.numel() > 0:
-                        #     gaussians.replace_tensor_to_optimizer(tex_old, "texture")
-                        #     gaussians.replace_tensor_to_optimizer(xyz_old, "xyz")
-                        #     gaussians.replace_tensor_to_optimizer(scale_old, "scaling")
-                        #     gaussians.replace_tensor_to_optimizer(rotation_old, "rotation")
-                        #     gaussians.replace_tensor_to_optimizer(opacity_old, "opacity")
-                else:
-                    pass
-
-                
-                # xyz_lr = gaussians.update_learning_rate(iteration)
-                
-                # L = build_scaling_rotation(gaussians.get_scaling, gaussians.get_rotation)
-                # actual_covariance = L @ L.transpose(1, 2)
-
-                # if iteration < 3000:
-                #     noise = torch.randn_like(gaussians._xyz) * (op_sigmoid(1- gaussians.get_opacity))*args.noise_lr*xyz_lr
-                # else:
-                #     # noise = torch.randn_like(gaussians._xyz) * (op_sigmoid(1- gaussians.get_opacity))*args.noise_lr*xyz_lr
-                # # noise = torch.randn_like(gaussians._xyz) * (op_sigmoid(1- gaussians.get_opacity)) * (op_sigmoid(1- gaussians.get_texture))*args.noise_lr*xyz_lr
-                #     noise = torch.randn_like(gaussians._xyz) * ((op_sigmoid(1- gaussians.get_opacity))+(op_sigmoid(1 - gaussians.get_texture)))*args.noise_lr*xyz_lr
-                
-                # noise = torch.bmm(actual_covariance, noise.unsqueeze(-1)).squeeze(-1)
-                # gaussians._xyz.add_(noise)
-                # gaussians._xyz=x_old
+
     total_sec = time.time() - train_start         # ←② 结束计时
     log_dir  = os.path.join(dataset.model_path, "timelog")
     os.makedirs(log_dir, exist_ok=True)
@@ -304,7 +250,10 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
 
     print(f"[TrainLog-1] total {total_sec/60:.2f} min  "
           f"(avg {total_sec/opt.iterations:.3f}s/it)")
-    print("Best PSNR = {} in Iteration {}".format(best_psnr, best_iteration))
+    if psnr_records:
+        print("Best PSNR = {} in Iteration {}".format(best_psnr, best_iteration))
+    if tb_writer is not None:
+        tb_writer.close()
 
 def random_choice(prob):
     return 1 if random.random() <= prob else 0
@@ -339,14 +288,14 @@ def training_report(tb_writer, iteration, Ll1, loss, l1_loss, elapsed, testing_i
         tb_writer.add_scalar('train_loss_patches/total_loss', loss.item(), iteration)
         tb_writer.add_scalar('iter_time', elapsed, iteration)
 
-    test_psnr = 0.0
+    test_psnr = None
     # Report test and samples of training set
     if iteration in testing_iterations:
         torch.cuda.empty_cache()
         validation_configs = ({'name': 'test', 'cameras': scene.getTestCameras()},
                               {'name': 'train',
                                'cameras': [scene.getTrainCameras()[idx % len(scene.getTrainCameras())] for idx in
-                                           range(5, 30, 5)]})
+                                           range(5, 30, 5)] if scene.getTrainCameras() else []})
 
         for config in validation_configs:
             if config['cameras'] and len(config['cameras']) > 0:
@@ -402,14 +351,20 @@ if __name__ == "__main__":
     parser.add_argument('--port', type=int, default=6009)
     parser.add_argument('--detect_anomaly', action='store_true', default=False)
     parser.add_argument("--test_iterations", nargs="+", type=int,
-                        default=[])
+                        default=None)
     parser.add_argument("--save_iterations", nargs="+", type=int, default=[1000, 7_000, 10_000, 20_000, 30_000, 40000])
     parser.add_argument("--quiet", action="store_true")
     args = parser.parse_args(sys.argv[1:])
-    interval = 2000
-    max_it   = args.iterations_lr          # lr阶段总迭代数
-    args.test_iterations = list(range(interval, max_it + 1, interval))
-    args.save_iterations.append(args.iterations)
+    # --iterations is the historic coarse flag; also honor --iterations_lr.
+    explicit_iterations = any(arg == "--iterations" or arg.startswith("--iterations=") for arg in sys.argv[1:])
+    if not explicit_iterations:
+        args.iterations = args.iterations_lr
+    args.iterations_lr = args.iterations
+    if args.iterations <= 0:
+        parser.error("--iterations/--iterations_lr must be positive")
+    if args.test_iterations is None:
+        args.test_iterations = list(range(2000, args.iterations + 1, 2000))
+    args.save_iterations = sorted(set(args.save_iterations + [args.iterations]))
 
     print("Optimizing " + args.model_path)
 
@@ -448,7 +403,7 @@ if __name__ == "__main__":
 
 
     # 写入前两项
-    metrics_path = os.path.join(dataset.model_path, "efficiency_metrics.json")
+    metrics_path = os.path.join(dataset.model_path, "efficiency_metrics_stage1.json")
     os.makedirs(dataset.model_path, exist_ok=True)
     with open(metrics_path, "w") as f:
         json.dump({
diff --git a/train_2.py b/train_2.py
index b7d2e53..1709f72 100644
--- a/train_2.py
+++ b/train_2.py
@@ -24,6 +24,7 @@ from utils.general_utils import safe_state, get_linear_noise_func, save_tensor_a
 import uuid
 from tqdm import tqdm
 from utils.image_utils import psnr
+from utils.tasa_utils import snapshot_gaussians, apply_tasa
 from argparse import ArgumentParser, Namespace
 from arguments import ModelParams, PipelineParams, OptimizationParams
 from scene.gaussian_model import build_scaling_rotation
@@ -43,7 +44,7 @@ except ImportError:
 def op_sigmoid(x, k=100, x0=0.995):
     return 1 / (1 + torch.exp(-k * (x - x0)))
 
-def training(dataset, opt, pipe, testing_iterations, saving_iterations):
+def training(dataset, opt, pipe, testing_iterations, saving_iterations, coarse_iteration=-1):
     # pdb.set_trace()
     train_start = time.time()
     tb_writer = prepare_output_and_logger(dataset)
@@ -57,12 +58,17 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
     
     
     deform_lr = DeformModel(dataset.is_blender, dataset.is_6dof)
-    deform_lr.load_weights_lr(dataset.model_path)
     
     deform_hr = DeformModel(dataset.is_blender, dataset.is_6dof)
     deform_hr.train_setting(opt)
     # pdb.set_trace()
-    scene = Scene(dataset, gaussians, load_iteration=-1, shuffle=False)
+    scene = Scene(dataset, gaussians, load_iteration=coarse_iteration, shuffle=False, stage="lr")
+    if not scene.getTrainCameras():
+        raise ValueError("The training split contains no cameras")
+    deform_lr.load_weights_lr(dataset.model_path, iteration=scene.loaded_iter)
+    deform_lr.deform.requires_grad_(False)
+    deform_lr.deform.eval()
+    coarse_before = {key: value.detach().cpu().clone() for key, value in deform_lr.deform.state_dict().items()}
     gaussians.training_setup(opt)
     bg_color = [1, 1, 1] if dataset.white_background else [0, 0, 0]
     background = torch.tensor(bg_color, dtype=torch.float32, device="cuda")
@@ -78,9 +84,9 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
     ema_loss_for_log = 0.0
     best_psnr = 0.0
     best_iteration = 0
-    progress_bar = tqdm(range(opt.iterations_lr), desc="Training HR progress")
+    progress_bar = tqdm(range(opt.iterations_hr), desc="Training HR progress")
     smooth_term = get_linear_noise_func(lr_init=0.1, lr_final=1e-15, lr_delay_mult=0.01, max_steps=20000)
-    for iteration in range(1, opt.iterations_lr + 1):
+    for iteration in range(1, opt.iterations_hr + 1):
         if network_gui.conn == None:
             network_gui.try_connect()
         while network_gui.conn != None:
@@ -92,7 +98,7 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
                     net_image_bytes = memoryview((torch.clamp(net_image, min=0, max=1.0) * 255).byte().permute(1, 2,
                                                                                                                0).contiguous().cpu().numpy())
                 network_gui.send(net_image_bytes, dataset.source_path)
-                if do_training and ((iteration < int(opt.iterations_lr)) or not keep_alive):
+                if do_training and ((iteration < int(opt.iterations_hr)) or not keep_alive):
                     break
             except Exception as e:
                 network_gui.conn = None
@@ -159,7 +165,7 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
         
         loss_sp = (1.0 - opt.lambda_dssim) * Ll1 + opt.lambda_dssim * (1.0 - ssim(image_lr, gt_image))
         # loss = (1.0 - opt.lambda_dssim) * Ll1 + opt.lambda_dssim * (1.0 - ssim(image_lr, gt_image))
-        loss = args.opacity_reg * torch.abs(gaussians.get_opacity).mean()
+        loss = opt.opacity_reg * torch.abs(gaussians.get_opacity).mean()
 
 
         
@@ -172,7 +178,7 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
         rendered_tex = render_pkg_edge["texture"]
         
         rendered_tex = rendered_tex.unsqueeze(0)
-        rendered_tex_lr = F.interpolate(rendered_tex, scale_factor=0.25, mode='bilinear', align_corners=False)
+        rendered_tex_lr = F.avg_pool2d(rendered_tex, kernel_size=4, stride=4)
         rendered_tex_lr = rendered_tex_lr.squeeze(0)
 
         mask_ppp = rendered_tex_lr[0].reshape(-1,1)
@@ -193,7 +199,7 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
                 progress_bar.set_postfix({"Loss": f"{ema_loss_for_log:.{7}f}",
                                           "num": gaussians.get_xyz.shape[0]})
                 progress_bar.update(10)
-            if iteration == opt.iterations_lr:
+            if iteration == opt.iterations_hr:
                 progress_bar.close()
 
             # Keep track of max radii in image-space for pruning
@@ -205,10 +211,13 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
                                        testing_iterations, scene, render, (pipe, background), deform_lr, deform_hr,
                                        dataset.load2gpu_on_the_fly, dataset.is_6dof)
 
-            if iteration in testing_iterations:
+            if iteration in testing_iterations and cur_psnr is not None:
+                current_psnr = float(cur_psnr)
+                if current_psnr > best_psnr:
+                    best_psnr, best_iteration = current_psnr, iteration
                 psnr_records.append({
                     "iteration": iteration,
-                    "psnr": round(cur_psnr.item(), 4)        # 保留 4 位小数即可
+                    "psnr": round(current_psnr, 4)        # 保留 4 位小数即可
                 })
 
             # if iteration in testing_iterations:
@@ -218,8 +227,11 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
 
             if iteration in saving_iterations:
                 print("\n[ITER {}] Saving Gaussians".format(iteration))
-                scene.save(iteration)
-                deform_hr.save_weights_hr(args.model_path, iteration)
+                scene.save(iteration, stage="hr")
+                deform_hr.save_weights_hr(dataset.model_path, iteration)
+                metadata_path = os.path.join(dataset.model_path, "deform_hr", f"iteration_{iteration}", "stage_metadata.json")
+                with open(metadata_path, "w") as f:
+                    json.dump({"coarse_iteration": scene.loaded_iter, "fine_iteration": iteration}, f, indent=2)
                 
 
                 # deform_lr.save_weights_lr(args.model_path, iteration)
@@ -237,99 +249,39 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
                         dataset.white_background and iteration == opt.densify_from_iter):
                     gaussians.reset_opacity()
 
-            xyz_old=gaussians._xyz
-            scale_old=gaussians._scaling
-            rotation_old=gaussians._rotation
-            opacity_old=gaussians._opacity
-            fea_1 = gaussians._features_dc
-            fea_2 = gaussians._features_rest
-            tex_old=gaussians._texture
-
-            # Optimizer step
-            if iteration < opt.iterations_lr:
+            # TASA must snapshot raw values before Adam mutates the Parameters.
+            # Fine-stage TASA is active from its first optimization step.
+            if iteration < opt.iterations_hr:
+                previous = snapshot_gaussians(gaussians)
                 gaussians.optimizer.step()
-                gaussians.update_learning_rate(iteration)
+                xyz_lr = gaussians.update_learning_rate(iteration)
                 deform_hr.optimizer.step()
                 gaussians.optimizer.zero_grad(set_to_none=True)
-                deform_hr.optimizer.zero_grad()
+                deform_hr.optimizer.zero_grad(set_to_none=True)
                 deform_hr.update_learning_rate(iteration)
 
-                xyz_lr = gaussians.update_learning_rate(iteration)
-                if not isinstance(xyz_lr, torch.Tensor):
-                    xyz_lr = torch.tensor(xyz_lr, device=tex_old.device)
-
-                tex_new=gaussians.get_texture
-                # pdb.set_trace()
-                mask = tex_new < tex_old
-                prob = torch.ones_like(tex_old, device=tex_old.device)
-
-                if mask.any():
-                    
-                    prob[mask] = (torch.exp(-(tex_old[mask] - tex_new[mask])* opt.tex_rate/xyz_lr))
-                    p_rand = torch.rand_like(prob, device=tex_old.device)
-                    # pdb.set_trace()
-                    p_nom = p_rand < prob
-
-                    # p_nom = torch.tensor([random_choice(p.item()) for p in prob], device=tex_old.device)
-
-                    # if p_nom.any() and iteration > 3000 :
-                    if p_nom.any():
-                        # print(p_nom.sum()/p_nom.shape[0])
-                        accept_rate = p_nom.float().mean().item()        # True=1, False=0
-                        if iteration % 100 == 0:                         # 每 100 步记录
-                            print(f"[HR iter {iteration}] accept_rate = {accept_rate:.6f}")
-                            with open(acc_log_hr, "a") as f:
-                                f.write(f"{iteration},{accept_rate:.6f}\n")
-                        p_nom = p_nom.squeeze()
-                        gaussians._xyz[~p_nom] = xyz_old[~p_nom]
-                        gaussians._features_dc[~p_nom] = fea_1[~p_nom]
-                        gaussians._features_rest[~p_nom] = fea_2[~p_nom]
-                        gaussians._texture[~p_nom] = tex_old[~p_nom]
-                        gaussians._scaling[~p_nom] = scale_old[~p_nom]
-                        gaussians._rotation[~p_nom] = rotation_old[~p_nom]
-                        gaussians._opacity[~p_nom] = opacity_old[~p_nom]
-                        # indices_to_restore = p_nom.nonzero(as_tuple=True)[0]
-                        # if indices_to_restore.numel() > 0:
-                        #     gaussians.replace_tensor_to_optimizer(tex_old, "texture")
-                        #     gaussians.replace_tensor_to_optimizer(xyz_old, "xyz")
-                        #     gaussians.replace_tensor_to_optimizer(scale_old, "scaling")
-                        #     gaussians.replace_tensor_to_optimizer(rotation_old, "rotation")
-                        #     gaussians.replace_tensor_to_optimizer(opacity_old, "opacity")
-                else:
-                    pass
-
-                
-                # xyz_lr = gaussians.update_learning_rate(iteration)
-                
-                # L = build_scaling_rotation(gaussians.get_scaling, gaussians.get_rotation)
-                # actual_covariance = L @ L.transpose(1, 2)
-
-                # if iteration < 3000:
-                #     noise = torch.randn_like(gaussians._xyz) * (op_sigmoid(1- gaussians.get_opacity))*args.noise_lr*xyz_lr
-                # else:
-                #     # noise = torch.randn_like(gaussians._xyz) * (op_sigmoid(1- gaussians.get_opacity))*args.noise_lr*xyz_lr
-                # # noise = torch.randn_like(gaussians._xyz) * (op_sigmoid(1- gaussians.get_opacity)) * (op_sigmoid(1- gaussians.get_texture))*args.noise_lr*xyz_lr
-                #     noise = torch.randn_like(gaussians._xyz) * ((op_sigmoid(1- gaussians.get_opacity))+(op_sigmoid(1 - gaussians.get_texture)))*args.noise_lr*xyz_lr
-                
-                # noise = torch.bmm(actual_covariance, noise.unsqueeze(-1)).squeeze(-1)
-                # gaussians._xyz.add_(noise)
-                # gaussians._xyz=x_old
-            # if iteration == opt.iterations_hr:
-            #     pdb.set_trace()
-            #     detach_data = {
-            #         "d_xyz": d_xyz.detach(),
-            #         "d_scaling": d_scaling.detach(),
-            #         "d_rotation": d_rotation.detach(),
-            #     }
-            #     torch.save(detach_data, args.model_path + "/training_lr_weights.pth")
+                accepted, _ = apply_tasa(gaussians, previous, xyz_lr, opt.tex_rate)
+                accept_rate = accepted.float().mean().item() if accepted.numel() else 1.0
+                if iteration % 100 == 0 or iteration == 1:
+                    print(f"[ITER {iteration}] accept_rate = {accept_rate:.6f}")
+                    with open(acc_log_hr, "a") as f:
+                        f.write(f"{iteration},{accept_rate:.6f}\n")
+
+    coarse_unchanged = all(torch.equal(coarse_before[key], value.detach().cpu())
+                           for key, value in deform_lr.deform.state_dict().items())
+    if not coarse_unchanged:
+        raise RuntimeError("The frozen coarse deformation network changed during fine training")
+    print("[Stage 2] Frozen coarse network unchanged: True")
     total_sec = time.time() - train_start         # ←②
 
     log_dir  = os.path.join(dataset.model_path, "timelog")
     os.makedirs(log_dir, exist_ok=True)
     with open(os.path.join(log_dir, "trainlog_stage2.json"), "w") as f:
         json.dump({"total_seconds": total_sec,
-                   "iterations":    opt.iterations_lr,
-                   "avg_sec_per_it": total_sec / opt.iterations_lr},
+                   "iterations":    opt.iterations_hr,
+                   "coarse_iteration": scene.loaded_iter,
+                   "coarse_parameters_unchanged": coarse_unchanged,
+                   "avg_sec_per_it": total_sec / opt.iterations_hr},
                   f, indent=2)
                   
     with open(psnr_log_path, "w") as f:
@@ -337,8 +289,11 @@ def training(dataset, opt, pipe, testing_iterations, saving_iterations):
     print(f"[TrainLog] PSNR log written to {psnr_log_path}")
 
     print(f"[TrainLog-2] total {total_sec/60:.2f} min  "
-          f"(avg {total_sec/opt.iterations_lr:.3f}s/it)")
-    print("Best PSNR = {} in Iteration {}".format(best_psnr, best_iteration))
+          f"(avg {total_sec/opt.iterations_hr:.3f}s/it)")
+    if psnr_records:
+        print("Best PSNR = {} in Iteration {}".format(best_psnr, best_iteration))
+    if tb_writer is not None:
+        tb_writer.close()
 
 def prepare_output_and_logger(args):
     if not args.model_path:
@@ -370,14 +325,14 @@ def training_report(tb_writer, iteration, Ll1, loss, l1_loss, elapsed, testing_i
         tb_writer.add_scalar('train_loss_patches/total_loss', loss.item(), iteration)
         tb_writer.add_scalar('iter_time', elapsed, iteration)
 
-    test_psnr = 0.0
+    test_psnr = None
     # Report test and samples of training set
     if iteration in testing_iterations:
         torch.cuda.empty_cache()
         validation_configs = ({'name': 'test', 'cameras': scene.getTestCameras()},
                               {'name': 'train',
                                'cameras': [scene.getTrainCameras()[idx % len(scene.getTrainCameras())] for idx in
-                                           range(5, 30, 5)]})
+                                           range(5, 30, 5)] if scene.getTrainCameras() else []})
 
         for config in validation_configs:
             if config['cameras'] and len(config['cameras']) > 0:
@@ -441,14 +396,18 @@ if __name__ == "__main__":
     parser.add_argument('--port', type=int, default=6009)
     parser.add_argument('--detect_anomaly', action='store_true', default=False)
     parser.add_argument("--test_iterations", nargs="+", type=int,
-                        default=[])
+                        default=None)
     parser.add_argument("--save_iterations", nargs="+", type=int, default=[7_000, 10_000, 20_000, 30_000, 40000])
     parser.add_argument("--quiet", action="store_true")
+    parser.add_argument("--coarse_iteration", type=int, default=-1, help="Coarse checkpoint to load; -1 selects the latest coarse stage")
     args = parser.parse_args(sys.argv[1:])
-    interval = 2000
-    max_it   = args.iterations_lr          # lr阶段总迭代数
-    args.test_iterations = list(range(interval, max_it + 1, interval))
-    args.save_iterations.append(args.iterations_hr)
+    if args.iterations_hr <= 0:
+        parser.error("--iterations_hr must be positive")
+    if args.coarse_iteration == 0 or args.coarse_iteration < -1:
+        parser.error("--coarse_iteration must be -1 or a positive checkpoint iteration")
+    if args.test_iterations is None:
+        args.test_iterations = list(range(2000, args.iterations_hr + 1, 2000))
+    args.save_iterations = sorted(set(args.save_iterations + [args.iterations_hr]))
 
     print("Optimizing " + args.model_path)
 
@@ -458,7 +417,7 @@ if __name__ == "__main__":
     # Start GUI server, configure and run training
     # network_gui.init(args.ip, args.port)
     torch.autograd.set_detect_anomaly(args.detect_anomaly)
-    # training(lp.extract(args), op.extract(args), pp.extract(args), args.test_iterations, args.save_iterations)
+    # training(lp.extract(args), op.extract(args), pp.extract(args), args.test_iterations, args.save_iterations, args.coarse_iteration)
 
     # —— 原有构造 —— 
     dataset = lp.extract(args)
@@ -487,7 +446,7 @@ if __name__ == "__main__":
 
 
     # 写入前两项
-    metrics_path = os.path.join(dataset.model_path, "efficiency_metrics.json")
+    metrics_path = os.path.join(dataset.model_path, "efficiency_metrics_stage2.json")
     os.makedirs(dataset.model_path, exist_ok=True)
     with open(metrics_path, "w") as f:
         json.dump({
@@ -501,7 +460,7 @@ if __name__ == "__main__":
     # —— 打桩统计结束 —— 
 
     # —— 原训练逻辑 —— 
-    training(lp.extract(args), op.extract(args), pp.extract(args), args.test_iterations, args.save_iterations)
+    training(lp.extract(args), op.extract(args), pp.extract(args), args.test_iterations, args.save_iterations, args.coarse_iteration)
 
     # —— 4) 训练结束后统计训练时长 & 峰值显存 —— 
     train_hours = (time.time() - t0) / 3600.0
